%h1 Products List
%ul
  - @categories.each do |category|
    .div{id: dom_id(category)}
      %h1= category.name
      - category.products.each do |product|
        %li
          = product.title
          = product.content
          = product.price